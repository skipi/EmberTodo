{{#with todo_item}}
  {{view Ember.TextField valueBinding="title" placeholder="Title"}}
  {{view Ember.TextField valueBinding="content"  placeholder="Todo content"}}
  <button type="submit">
    {{#if id}}Update{{else}}Create{{/if}}
  </button>
{{/with}}
<a href="#" {{action "cancelForm"}}>Cancel</a>